{
  "name": "upload-document-tools",
  "version": "0.1.0",
  "description": "Small tools for upload_document testing (sign-in + print org_id)",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node -r dotenv/config ./scripts/login_and_print_org.mjs",
    "start-ui": "node ./scripts/generate_frontend_env.mjs && node -r dotenv/config ./server/index.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.28.0",
    "dotenv": "^16.1.4",
    "serve": "^14.2.0",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "uuid": "^9.0.0"
  }
}
